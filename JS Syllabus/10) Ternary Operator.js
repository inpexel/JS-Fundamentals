let weather = "sunny";

let message = weather === "sunny" ? "beautyfull day!"
            : weather === "cloudy" ? "bit overcast"
            : weather === "rainy" ? "heavy rain"
            : "sorry don't recognize that weather condition";

console.log(message);