/*
Template strings (also known as template literals) are a way to
create strings in JavaScript that allows for embedding expressions
inside of them. They use backticks (`) instead of single quotes ('') or double quotes ("").
*/

let name = "Alice";
let greeting = `Hello, ${name}!`;
console.log(greeting); // logs "Hello, Alice!"