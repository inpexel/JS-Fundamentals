function checkVariables(){
    let name = 'vishal';
    var about = "vishal is an Artist"

    return console.log(`'name :- '${name} "About :- " ${about}`);
}

checkVariables()

/*3 ways to define variables 
    var, let, const 

Variables can hold different types of values, including strings,
numbers, booleans, arrays, objects, and more. You can also assign null or undefined
to a variable if you want to indicate that it has no value.
*/  
